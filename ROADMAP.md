# Roadmap

No future features are defined yet. This project is a faithful extraction of the existing tracker, not a redesign.

## Known next step (from the owner)

- **Logic refactor** — a separate refactoring stage is planned *after* this extraction is validated in a browser. The current code deliberately preserves the original behavior and structure, so any refactor should start from a verified baseline (see `docs/MIGRATION_REPORT.md`).

Anything beyond that is undecided.
